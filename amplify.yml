version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - npm run build --workspace web
      artifacts:
        baseDirectory: apps/web/.next
        files:
          - "**/*"
      # 🚫 quita cache por completo
      # cache:
      #   paths:
      #     - apps/web/.next/cache/**/*
      #     - .npm/**/*
      buildPath: /
    appRoot: apps/web